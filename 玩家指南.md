# 猫九的滑块游戏 · 玩家指南

把打乱的方块**滑回一个完整的实心矩形**（m×n，或横过来的 n×m）就算复原。

> 目标是「任意位置的完整矩形」，**不是回到打乱前的布局**——矩形出现在哪里都行。
>
> 还有三种变体：**带序号**（编号也要归位）、**正三角形密铺**和**米字格**（见文末）。

## 启动

- 双击 exe，或运行 `python main.py`
- 菜单「谜题」可选择尺寸、步长（step）并打乱

## 三种操作方式（菜单 设置 → 控制，可任意开关组合，默认全开）

| 模式 | 怎么玩 |
| --- | --- |
| **单次触控** | 直接按住一个滑块，朝想滑的方向**拖动**，松手即滑动。最省事 |
| **两次触控** | 先点一条**缝隙**，再点缝隙一侧的方块选中整组，然后拖动或按方向键 |
| **鼠标键盘** | 选中后用 **方向键 / W A S D** 移动（三角形谜题为六向：W E / A D / Z X） |

- 拖动空白处 = 平移视角；Ctrl + 滚轮 = 缩放
- 右键 = 取消选中
- 滑动失败时右下角会提示原因：
  - **会断开**：这样滑，方块组会被扯成不相连的两部分，不允许
  - **会重叠**：滑块会撞到别的方块，不允许
  - **方向不对**：缝隙方向与移动方向不匹配（三角形／米字格谜题每族缝只认两个平行方向）

## 辅助功能（设置 → 动画速度页）

- **分组着色**：步长 step>1 时开启。同色描边的方块属于同一组，只有同组方块能互相换位——颜色就是移动规律的提示（目前方形／带序号可用，三角形与米字格的版本在计划中）
- **悬停连锁提示**：鼠标悬停时，高亮同组的所有位置（包括空格），帮你判断「这个洞该由哪块补」（方形与三角形可用，米字格的版本在计划中）

## 常用快捷键

| 按键 | 功能 |
| --- | --- |
| Ctrl+Z / Ctrl+X | 撤销 / 重做 |
| Alt+S | 打乱 |
| Ctrl+R | 重置（回到打乱前） |
| Ctrl+S / Ctrl+O | 保存 / 打开存档 |
| Ctrl+A | 自动求解（带序号 / 三角形形态停用） |
| Ctrl+M | 开始/停止录制宏 |
| F1 / F2 / F3 | 虚拟键盘 / 调试面板 / 成绩面板 |
| W E / A D / Z X | 三角形形态的六个滑动方向（↖↗ / ←→ / ↙↘） |
米字格的八个方向（W E / A D / Z X / Q S）不绑物理键，只出现在虚拟键盘（F1）上——见文末「米字格形态」。

## 小贴士

- step>1 时每次移动整组平移 step 格，这是规则的一部分，不是卡顿
- 滑块可以滑到棋盘边界之外，视角跟不上时拖动空白处找一找
- 撤销回复原状态不会再弹复原提示，属于正常设计

## 正三角形密铺形态

在「謎題」菜單選三角形預設（`1~tri4` / `2~tri6` / `2~tri8`），或命令行 `python main.py 6 6 1 tri`（第一個數字是大三角邊長 k）。與普通方塊版的區別：

- 棋盘由 k² 个小正三角形拼成大三角，开局就是一个完整的大三角（还原态），按 `Alt+S` 打乱
- **缝隙有三族**：水平、+60°、−60°
- **滑动有六个方向**：键盘 `W E / A D / Z X` 正好是六边形排列；移动沿着缝隙线走，每族缝只认两个方向，按错键会提示「方向不对」
- 直接按住滑块拖动也行：拖动方向会自动吸附到六个方向里最接近的那个
- 复原目标 = 拼回任意位置的实心大正三角形（可整体平移、可转 120°，颠倒不行）
- 虚拟键盘（F1）在三角形形态下是六向三行布局，和键盘按键一一对应
- 求解器、宏、创造模式在此形态暂不开放；竞速成绩、存读档、悬停连锁提示照常可用

## 米字格形态

在「謎題」菜單選米字格預設（`1~mi4*4` / `2~mi4*4` / `2~mi6*6` / `2~mi8*8`），或命令行 `python main.py 4 4 1 mi`。每個方格被兩條對角線切成 4 個三角滑塊，與普通方塊版的區別：

- **「一格」按方向定義**：橫向／縱向一格 = 一條格邊的長度；斜向一格 = 半個格身的斜向距離（約 0.707 倍格邊）。也就是說斜著走的「一步」比橫豎走的「一步」短，這是規則，不是卡頓
- 等级 2 的斜向 = 連走兩次斜向一格，長度正好等於舊版的整格斜距
- **縫隙有四族**：方格邊（橫／縱）和兩條對角線；**滑動有八個方向**（W E / A D / Z X，外加 Q 與 S——這些鍵位只出現在虛擬鍵盤上，物理鍵盤不綁）
- 操作走**兩次觸控**：先點一條縫，再**按住想動的那一側的滑塊拖動**——拖動的位移在縫隙方向上的偏向決定往哪邊滑，鬆手才移動。第二下只按不拖 = 只選中這一組，方向留給虛擬鍵盤（F1）。不做單次觸控：八個方向裡靠一次觸控猜方向太容易誤觸
- 斜著走過一次之後，棋盤會「錯開半格」——這時很多橫豎縫會選不動（那條線剛好穿過方塊內部）。這是正常狀態，沿斜向再走一格就能錯回來；提示會告訴你這族縫隙還剩幾條
- 復原目標 = 拼回實心矩形，**錯開半格的位置也算對**（一條斜縫兩半朝同向各走一格就能換過去，是普通玩法走得到的狀態）
- 求解器、宏、創造模式、分組著色與調試面板在此形態暫不開放；競速成績、存讀檔、打亂、撤銷重做照常可用
