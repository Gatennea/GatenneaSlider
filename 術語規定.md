# 貓九的滑塊遊戲 — 術語規定

> 目的：供各開發者與 AI Agent 交流時統一用語，防止互不理解。
> 基準：遊戲規則/求解器模塊 docstring、`gui/操作说明.md`、`readme.md` §2。

***

## 1. 基礎對象

| 術語           | 定義                                                          |
| ------------ | ----------------------------------------------------------- |
| 滑塊 Block     | 正方形單元，`location=[r,c]`（0-based，row 向下為正）                    |
| 滑塊組          | `m×n` 起始版面的**整體**（替代「棋盤」一詞；交流時不用「棋盤」）                       |
| 謎題 puzzle    | 由 (m,n,step) 決定；標籤 `"{step}~{m}*{n}"`，例 `2~4*4`，成績/宏/建表按它分組。變體：帶序號加 `#num`、三角形密鋪為 `"{step}~tri{k}"`、米字格為 `"{step}~mi{m}*{n}"` |
| 等級 / 步距 step | 每次移動的距離單位；選中組整體平移 step 的整數倍。**米字格按族定義「一格」**（見 §9.6）：step 是「幾個一格」，不是固定像素長度 |
| solved / 還原  | 所有方塊構成**任意位置**的 `m×n` 或 `n×m` 實心矩形（允許平移/轉置，**不是回到初始版面**）；三角形密鋪為任意位置的邊長 k 實心大正三角 |

## 2. 一次完整移動

固定三段，缺一不可：`select_gap(選縫隙) → select_block(點一塊) → move(方向)`。

| 術語           | 定義                                                                                                |
| ------------ | ------------------------------------------------------------------------------------------------- |
| 縫隙 gap       | 兩區塊間分割線；`h`=橫縫（行間，只能左右 a/d），`v`=縱縫（列間，只能上下 w/s）；以 `line` 標號定位                                     |
| 選中組          | `select_block` 點選縫隙一側與目標塊**連通**的那整片方塊（同側 DFS，`game.opt()`）；側別 side：h 用 above/below、v 用 left/right |
| 方向鍵          | `w`上 / `s`下 / `a`左 / `d`右（三角形密鋪為六向：`w`↖ / `e`↗ / `a`← / `d`→ / `z`↙ / `x`↘，見 §12）                         |
| Action（動作編碼） | 四元組 `(gap_type, gap_line, side, move_dir)`；合法動作 = 有效縫隙 × 2 側 × 2 垂直方向                             |
| 單一連通不變量      | 全組方塊任何時刻須整體連通；移動造成斷開/重疊即非法（看似不能動不是 bug）                                                           |

> side 邊界細節：above/left 含 line 本身（`<=`），below/right 不含（`>`）。

## 3. 著色器・連鎖器・mod 不變量（同一規律的三個面）

| 名稱                   | 是什麼                                                                                                             |
| -------------------- | --------------------------------------------------------------------------------------------------------------- |
| mod 不變量（代碼亦稱「著色不變量」） | step>1 時，每次合法移動使每塊 `(r%step, c%step)` **永不變**。推論：方塊被分成 step×step 個「組」，方塊只能在同組內互換，不能跨組；還原矩形左上角只能在特定 mod 網格。**只適用方形與三角形**：米字格的斜向一格是 ±(½,½)，座標會落到半整數上，`r%step` 跟著在 0 與 0.5 之間翻，米字格也不做著色/連鎖/求解器       |
| 著色器                  | **靜態視覺化**：`_group_color` 按 `(r%step,c%step)` 決定描邊色，同組恆同色 → 「同色＝同組＝可互換」                                          |
| 連鎖器                  | **動態查詢提示**：懸停某格（含空格）時，邊界盒內同組位置（含空格）提亮 → 判斷洞該由哪個同色凸起補                                                            |
| 求解器側                 | 同一規律當硬約束：`detect_target_corner` 依 mod 計數預判窗口左上角；`find_best_window` 只保留 mod 合規窗口；fill\_macro 要求洞與凸起**同 mod 才可填** |

一句話：著色器＝靜態展示「組」，連鎖器＝動態查詢「組」，求解器＝把「組」當約束。

## 4. 目標窗口與「洞 / 凸起」

- 目標窗口：分數對應的矩形 `region=(r0,c0,(rh,cw))`，由 `find_best_window()` 依 mod 約束唯一決定；調試面板畫框與求解器必須共用同一結果。

| 術語                | 定義                                                                  | 標記  |
| ----------------- | ------------------------------------------------------------------- | --- |
| 洞 hole            | 目標窗口**內的空格**（被包圍）                                                   | 圓圈  |
| 缺口 dent           | 目標窗口內、連到框緣的空格（**代碼一律用 'dent'，舊 'gap' 已廢**；中文仍叫「缺口」，與「縫隙 gap」不再同名異義） | 三角形 |
| 凸起 protrusion / p | 目標窗口**外的方塊**                                                        | 菱形  |
| 單洞單凸              | 恰好 1 洞 + 1 凸起的局面（fill\_macro 可解）；洞與凸必須**同 mod** 才可能填                | —   |

> 空位計數口徑：**總空位（格數）＝ 孔洞數 + 缺口數**，皆按檢測器的「連通簇」計（一格空位＝1 簇）。GUI「隨機生成」把總空位拆成「孔洞數 / 缺口數」兩欄輸入，就是要精確控制簇類別。

## 5. 宏與填洞宏

| 術語             | 定義                                                                       |
| -------------- | ------------------------------------------------------------------------ |
| 宏              | 玩家自定義操作序列（像魔方公式）；錄製記錄**相對**基準滑塊的步驟；執行時選新基準；可 step 相容拆分；可逆序播放             |
| rep\_cell（代表格） | 每步宏動作的附帶格：移動**前**的位置，供精確回放（commit 後 location 已變）                         |
| 逆序宏 A′         | 同縫同側、方向取反、目標取該側首塊（= GUI 逆序播放語義）                                          |
| fill\_macro 階段 | setup（同行錯開）→ A（凸起送到洞列）→ B（豎向推入洞）→ A′（逆序還原）。腳本注釋稱被整條推移的選中組為「帶」，交流時統稱「選中組」 |

## 6. 視圖旋轉（少用，知道語義即可）

- 目的：把「凸起在窗口任意一側」統一旋轉成「凸起在右側」再解，解完逆旋輸出。
- 旋轉名：`id` / `r180` / `cw` / `ccw`；旋轉後窗口尺寸可能變 (n,m)。
- 旋轉在窗口局部坐標系進行，產出的動作與代表格會逆映射回世界絕對座標。

## 7. 控制通道

- 主通道：HTTP REST `127.0.0.1:5050`（`POST /command` 包 CLI 指令）。
- 次通道：stdin（打包 `--windowed` 時自動停用）。
- status schema 欄位（puzzle / step\_count / solved / matrix / selected\_gap / selected\_block / animating）為跨介面共用語言。

## 8. 不作為交流用語的內部術語

| 內部術語                      | 說明                                               |
| ------------------------- | ------------------------------------------------ |
| map 文本（`#`/`_`/`^`/`v`） | 執行期導入導出格式（`export_map()/import_map()`），**不作為日常交流用語**；交流形狀時直接說局面 |
| state\_key / canonicalize | 求解內部狀態編碼，交流不提                                    |
| origin / view / world 空間  | view\_rotate 內部坐標系，交流不提（改用「世界絕對座標」「窗口局部座標」等普通說法） |

## 9. 易混對照

| 易混點               | 正確用法                                          |
| ----------------- | --------------------------------------------- |
| 滑塊組 vs 選中組        | 滑塊組＝整體；選中組＝被移動的那片連通方塊（GUI 通知「选中滑块组」指後者，依語境區分） |
| gap 縫隙 vs dent 缺口 | 已分名：縫隙的操作編碼恆為 `gap_*`，缺口恆為 `dent`，不再同名異義      |
| 缺口、孔洞 vs 凸起       | 缺口、孔洞＝空位（沒有滑塊的地方）、凸起＝方塊，皆相對目標窗口               |
| 還原 vs 重置          | solved ≠ 回到初始布局；reset（重置）＝回到打亂前               |
| h/v 與移動方向         | h 縫隙→a/d；v 縫隙→w/s（垂直關係）；**三角形為平行關係**（h→a/d、p→e/z、n→w/x），勿混             |
| side 邊界           | above/left 含 line（`<=`）；below/right 不含（`>`）   |

## 9.5 正三角形密鋪形態術語（2026-09-20 新增）

實作見 `game_triangle.py`（純邏輯）與 `gui/triangle_view.py`（幾何視圖）。建立謎題：`new m n step tri`（`m` 作邊長 k）。

| 術語 | 定義 |
| --- | --- |
| 單位三角 / 菱形胞 | 最小單元是**尖朝上 ▲(i,j)** 或 **尖朝下 ▼(i,j)** 的單位正三角形，以斜座標 `(i, j, up)` 標號；相鄰 ▲▼ 合成一個菱形胞 `R(i,j)`。一片 ▲ 恰有 3 個 ▼ 鄰居（交錯相鄰），選中組按此 3-鄰接 DFS |
| 三族縫隙 | 縫隙是穿過密鋪的直線，按方向分三族：`h` 水平線（`j=line`）、`p`（`i=line`，+60°）、`n`（`i+j=line`，−60°）；`select_gap h/p/n line` |
| 六向 wedxza | 移動**平行**於縫隙線，共 6 向：`w`↖ / `e`↗ / `a`← / `d`→ / `z`↙ / `x`↘（鍵盤上正好排成六邊形）。每族縫只認兩個方向：`h`→`a/d`、`p`→`e/z`、`n`→`w/x`；其餘回 `wrong_direction` |
| 大三角 / k² | 邊長 k 的實心大正三角 = k² 個單位三角。建局即還原態；打亂另按 `Alt+S` |
| 三角還原 | 全部 k² 片拼成任意位置的大正三角：**可平移、可轉 120°/240°；鏡像（顛倒）不可達**。判定用頂點集凸包（三頂點、三邊長皆 k、殼內單元一致） |
| 三角 mod 不變量 | `(i % step, j % step)` 永不變（三族移動向量都是 step 整數倍）；連鎖提示按 `(i%step, j%step)` 高亮，是方形 `(r%step,c%step)` 的類比 |
| 三角 map 文本 | `#`=▲▼俱全、`^`=僅▲、`v`=僅▼、`_`=空白（菱形胞 2-bit：0/1/2/3）。方形的 `#/_` 圖**不可**餵給三角形，反之亦然 |
| 暫不支援 | 求解器/建表/ML（需新動作語義與對稱群）、`/analysis/*`（回 400）、宏與創造模式、著色與調試面板三角版（P3）。競速/成績/存讀檔/六向拖拽/虛擬鍵盤/連鎖提示可用 |

> 互動設計要點：鍵盤六鍵只是捷徑；主路徑是單次觸控拖拽——手勢只給一個向量，投影吸附到 `DIRECTION_SCREEN` 六個方向中點積最大者。日後若出現 12 向的新形狀，只需擴充 `DIRECTIONS` / `GAP_DIRECTIONS` 兩張表，互動代碼不改。

## 9.6 米字格形態術語（2026-09-23 新增）

實作見 `game_mi.py`（純邏輯）與 `gui/mi_view.py`（幾何視圖）。建立謎題：
`new m n step mi`，標籤 `{step}~mi{m}*{n}`，菜單裡寫成「米字 4×4 等級1」。

| 術語 | 定義 |
| --- | --- |
| 單位塊 / 米字格 | 一個方格沿兩條對角線切成 4 個直角等腰三角，每片是一個滑塊，以 `(r, c, q)` 標號（`q` = 斜邊貼著哪條格邊：`N`/`E`/`S`/`W`）。整塊 m×n = 4mn 片 |
| 四族縫隙 | 縫隙是四族直線：`h` 橫格邊、`v` 豎格邊、`d1` 主對角（`\`）、`d2` 副對角（`/`）；`select_gap h/v/d1/d2 line` |
| **一格（用戶口徑 2026-09-23）** | **一格 = 該方向能滑動的最小量，與幾何長度無關**。橫豎一格 = 一條格邊（長 1）；斜向一格 = 半個格身的斜向距離（√2/2，座標上平移 ±(½,½)）。**不存在「半格滑動」這種說法**：斜向那一步就是一格；舊版的整格斜距 ±(1,1) 叫作**斜向兩格**。報長度時就報長度（√2/2），不折算成「幾格」 |
| 八向 wedxzaq | 移動**平行**於縫隙線，共 8 向：`w`↖ / `e`↗ / `a`← / `d`→ / `z`↙ / `x`↘ / `q`（\ 縫反向）/ `s`（僅豎縫）。每族縫只認兩個方向：`h`→`a/d`、`v`→`w/s`、`d1`→`x/q`、`d2`→`e/z`；其餘回 `wrong_direction` |
| 等級 2 斜距 | step=2 的斜向 = 連走兩次斜向一格 = ±(1,1)，**正是不改版（整格斜距）的等級 1**。等級語義因此漂移：等級 1 變短、等級 2 維持舊手感 |
| 錯位態 / 兩個晶格 | 斜向一格把一半的塊推出整數座標，從此存在兩個晶格：**A**（整數座標，建局態）與 **B**（半整數座標）。不變量：`2r` 與 `2c` 同奇偶，故 `r−c`、`r+c` 恆為整數 → 對角縫永遠切不進塊裡，只有橫豎縫會失效 |
| 錯位態的橫豎縫 | 半整數層級的格邊整條穿過塊的內部 → 選不動、也滑不動。**不是全鎖**：6×6 沿 d1 走一格後 h 還剩 3 條、v 還剩 2 條，提示按實際剩餘條數說；把任一半再沿斜向推一格就回到單一晶格，橫豎縫全數復活 |
| 米字還原 | 全部 4mn 片拼成任意位置的實心 m×n 米字矩陣：**可平移、可換晶格（錯開半格也算對）**。判據是 8-bit 矩陣的形狀（bit0~3 = A 晶格四個 q、bit4~7 = B 晶格），不管落在哪個晶格 |
| 米字 map 文本 | 每格兩個十六進制字元（A/B 兩個 nibble 併成 8 bit），帶 `mi8` 標記行；舊的每格一字元（4-bit）匯入端照樣吃，匯出端沒有半整數塊時仍輸出舊格式 |
| `/status` 的米字欄位 | blocks 每條帶 `q`（朝向）與 `lat`（`A`/`B` 晶格記號），**沒有 `mod`**：錯位態的 `r%step` 會在 0 與 0.5 之間翻，不再是那種不變式 |
| 控制與暫不支援 | 兩次觸控（第一下點縫、第二下點塊，方向由兩下位移在縫切向上的符號定）+ 虛擬鍵盤；**單次觸控與拖拽按凍結決策一律禁用**。求解器/建表/ML、`/analysis/*`、宏錄製執行、著色與調試面板暫不支援；競速/成績/存讀檔/打亂/撤銷重做可用 |

> 一句話：**米字格「一格」按族定義，斜向那步就是一格（√2/2），錯位態只是換了晶格、不是壞了**。

## 10. 對話勘誤附錄（AI 曾誤解的術語 / 口徑）
> 這些是在 fill\_macro 開發對話中被用戶多次解釋、且我一度理解錯誤的點，列出**勘誤**以免再犯。

| 術語             | 正確語義                                                                               | 我犯過的錯                                                  |
| -------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 歸一化            | **只做旋轉**：把「凸起相對窗口」翻到窗口右側（`cw`/`ccw`/`r180` 視方位），求解完再逆旋回世界座標；不負責找洞/凸起，也不是整套 view 圖層 | 以為旋轉「對 A′ 失敗無幫助」，還以為只有凸起在上/下才需要轉置；實際「旋轉就夠了」且是通用第一步     |
| 單層 vs 雙層共軛公式   | 單層 `A B A′` 是**填洞**；雙層 `A B C B′ A′` 是**填缺口**用的方案                                  | 曾把雙層公式拿來解單洞，混淆用途                                       |
| setup（預備段）     | 只需讓洞/凸起所在行**錯開（不同行即可）**，為 B 讓出豎向空間；沒有「補償缺失方向」的職責                                   | 曾把失敗歸因於「沒做 setup 的方向補償」，其實是歸一化方向先錯                     |
| A 段（水平段）       | 把凸起送到**與洞同列**；循環停止＝水平對齊。洞在反側時用**相對移動**（移對側連通片、等效反向），不硬移洞那側以免把洞帶走                   | 在「X 右移 vs Y 左移」等價上繞圈，一度想引入搜索                           |
| B 段終止條件        | 洞與凸起**重合**                                                                         | 曾以「洞消失／洞類型變缺口」當終止（那是洞身份追蹤缺失造成的誤判）                      |
| B 段縫隙/側選擇      | 先試「凸起與主體之間、凸起窄側」的縫（如 `(v, c-1, 'right')`），目標選凸起所在側 → 只帶動凸起（帶）                      | 選成橫跨過寬的連通片（俗稱「撕帶」），等於把一大片一起搬、沒真正選中凸起                   |
| 逆序宏 A′         | 直接複用遊戲「逆序宏」語義：同縫同側、方向取反、目標＝該側首塊                                                    | 想自己另造一套逆序，其實可直接複用                                      |
| rep\_cell（代表格） | 記錄移動**前**的格位；commit 後 `location` 已變，精確回放靠代表格定位，找不到才退回「該側首塊」                        | 用 commit 後的 location 去選目標，造成錯位                         |
| 方向對稱規則         | 算法只寫「洞在凸**上方**」分支；洞在凸**下方**時，A/B 上下方向全部取反即可                                        | 誤以為上下兩種情況是兩個獨立子算法                                      |
| 單洞測試口徑         | 最低目標＝「單洞打亂必還原」；測試池只應生成 `hole≥1、dent=0` 的局                                          | 把「單缺口（dent=1）」局面混進單洞失敗樣本——單缺口本就不該在當前測試範圍，失敗是「預期內」非 bug |
| 洞/凸起身份追蹤       | 整段 `A B A′` 認「同一個洞 + 同一顆凸起」實體；相對移動不換對象                                             | 期間重新做類型檢測，把「同一空位」誤判成「洞變缺口」                             |

> 一句話總結：**歸一化=旋轉、A=同列、B=重合、A′=逆序宏；洞/凸起從頭到尾認實體**。

## 11. 缺陷形態與進階解法語義（立起 / 剛體凸起 / 拼大洞）

> 用戶定義（2026-09-06），範例見 `save/example.json`（9×9 step2，16 步人類解）與
> `save/2-7-7-test.json`（需「中性整形預演」才可提升的多洞卡點）。

| 術語 | 定義 |
| --- | --- |
| 立起（立起的凸起） | 多個**相鄰**凸起作為一個**連通剛體**、無法各自滑動的形態。判定：以這組凸起為目標做旋轉歸一化後，它們都位於目標框右側、**同一行不同列**（如 example 中 row4 的 `(-2,-1)` 相鄰兩格＝水平多米諾）。缺陷根因：單洞 couple 宏假設「凸起可被單獨 B 推入」，而立起時任何移動都拖動整片連通 → 現算法無法填 |
| 拼大洞 | 立起缺陷的解法構想：先把剛體對應的**多個洞揉成一個形狀相容的相鄰大洞**（大洞＝剛體凸起的平移複製品，逐格 parity 一致），再仿單洞宏用共軛子把整片剛體一次性推入。example.json 的 16 步正是先大幅揉形（overlap 一度降到 61）再收攏的過程 |
| 剛體填洞宏 | 「單洞宏」泛化成「k-細胞剛體宏」：A＝調度整片剛體、B＝整體平移入洞、A′＝復位；前提是洞形恰為剛體形狀的 parity 相容副本 |
| 連鎖剛體 | 更大的相鄰凸起團（非單一剛體）；可拆子集 × 可併洞形的組合爆炸 → 連拼大洞也無法完全解決 |

> 分層認識（2026-09-06 用戶確認）：**規則剛體**（同排/同列相鄰、形狀規則，如水平多米諾）與
> **連鎖剛體**（更大的相鄰凸起團）分開對待——R0/R1 只針對規則剛體；連鎖剛體自 R2 起另議。

一句話：**凸起能單獨滑→單洞宏；只能整片滑→立起，需「拼大洞＋剛體填洞宏」；團塊越大公式族越散，是當前宏體系的已知邊界**。


